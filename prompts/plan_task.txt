## Tổng quan — 6 nhiệm vụ lớn, 18 bước nhỏ

---

### Nhiệm vụ 1 — Schema + State (2 bước)
```
Bước 1.1: Thêm PlanStep, Plan vào State.py
Bước 1.2: Thêm current_plan, plan_mode vào State(TypedDict)
```

---

### Nhiệm vụ 2 — Mẫu kế hoạch (3 bước)
```
Bước 2.1: Viết mẫu phòng Đạo Ôn 14 ngày
Bước 2.2: Viết mẫu phòng Khô Vằn 10 ngày  
Bước 2.3: Viết mẫu tổng hợp đa bệnh 21 ngày
→ Lưu vào prompts/plan_templates.txt
```

---

### Nhiệm vụ 3 — Planning Tools (4 bước)
```
Bước 3.1: create_skeleton_tool — tạo sườn từ mẫu + retrieve_tool
Bước 3.2: fill_detail_tool — điền chi tiết từng bước trong sườn
Bước 3.3: edit_plan_tool — chỉnh sửa theo yêu cầu user
Bước 3.4: save_plan_tool — lưu vào SQLite
→ File: src/tools/planning_tools.py
```

---

### Nhiệm vụ 4 — SQLite Schema (2 bước)
```
Bước 4.1: Tạo bảng plans trong SQLite
Bước 4.2: Viết PlanStore (CRUD) — create, read, update
→ File: src/memory/plan_store.py
```

---

### Nhiệm vụ 5 — Planning Agent (4 bước)
```
Bước 5.1: Viết prompt PLANNING.txt
Bước 5.2: Tạo planning_agent.py — ReAct với planning tools + retrieve_tool + weather_tool
Bước 5.3: Xử lý multi-turn — nhớ plan_state giữa các bước
Bước 5.4: Xử lý khi user thắc mắc → flag để router chuyển sang RAG Agent
```

---

### Nhiệm vụ 6 — Router + Kết nối (3 bước)
```
Bước 6.1: Sửa router_agent — nhận diện planning intent → route=2
Bước 6.2: Sửa run_agent.py — kết nối planning_agent vào graph
Bước 6.3: Test end-to-end toàn bộ luồng
```

---

## Thứ tự thực hiện

```
1.1 → 1.2 → 2.1~2.3 → 4.1 → 4.2 → 3.1~3.4 → 5.1 → 5.2 → 5.3 → 5.4 → 6.1 → 6.2 → 6.3
```

Bắt đầu từ **Bước 1.1** không?